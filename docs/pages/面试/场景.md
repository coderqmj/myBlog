### 1.输入框搜索，防抖和节流有什么区别

```
场景：百度搜索，抖音搜索视频
问题：
	1.不作任何处理，每一次字符改变都会触发onChange请求，服务器造成压力，用户体验不好
	2.导致竞态问题，就是先发出去的旧请求，把新请求的结果覆盖了，展示的数据不符合预期
解决方案：
	1.使用debounce，一段时间没有新的输入，就触发请求，也可以实现第一次就触发
	2.使用throttle，每隔一段频率的输入就去请求一次
```

