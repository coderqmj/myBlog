## 四、面试

### 1.混合使用

```
1.服务端组件里面可以使用客户端组件
2.但是客户端组件不能使用服务端组件

3.可以使用props的形式，在客户端组件中使用服务端组件

function ClientComponent ({ children}) {
	return (
		<>
			111
			{children}
		</>
	)
}
```

### 2.什么是水合



### 3.Image适配

```
1.可以使用img的srcset属性，可以适配2种情况：
	1.相同尺寸的不同分辨率。
	2.相同内容不同的宽高
```

### 4.CSR，SSR的特点

### 5.RSC模式怎么开启

### 5.suspense会影响SEO吗

### 7.streaming流式渲染

### 8.在 Client Component 中使用 Server Component 的数据缓存

```
1.使用props，因为服务端组件内是可以嵌套客户端组件的，把数据通过props传入客户端组件
2.
```

